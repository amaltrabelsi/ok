name: worldsoft_flight_booking
description: Application de réservation de vols Worldsoft 
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  http: ^1.2.2

  provider: ^6.1.2

  intl: ^0.20.2

  shared_preferences: ^2.3.3

  cupertino_icons: ^1.0.8

  cached_network_image: ^3.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/fonts/
